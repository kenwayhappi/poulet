import 'package:flutter/material.dart';

class NotificationsPageArtisan extends StatelessWidget {
  const NotificationsPageArtisan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Notifications Page Artisan',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
