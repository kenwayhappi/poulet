import 'package:flutter/material.dart';

class MorePageArtisan extends StatelessWidget {
  const MorePageArtisan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'More Page Artisan',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
