import 'package:flutter/material.dart';

class ToolsPageArtisan extends StatelessWidget {
  const ToolsPageArtisan({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Tools Page Artisan',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
